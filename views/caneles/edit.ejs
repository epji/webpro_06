<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>カヌレ記録編集</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="caneles-page">
    <div class="caneles-hero">
      <h1>✏️ カヌレ記録編集</h1>
      <p class="sub"><%= canele.shop %> / <%= canele.flavor %>（ID: <%= canele.id %>）</p>
    </div>

    <div class="caneles-formWrap">
      <form action="/caneles/<%= canele.id %>/update" method="POST" class="caneles-form">
        <div class="caneles-formGrid2">
          <div>
            <label>ショップ</label>
            <input type="text" name="shop" value="<%= canele.shop %>" required>
          </div>
          <div>
            <label>エリア</label>
            <input type="text" name="area" value="<%= canele.area %>" required>
          </div>
        </div>

        <div class="caneles-formGrid2">
          <div>
            <label>価格（円）</label>
            <input type="number" name="price" value="<%= canele.price %>" required>
          </div>
          <div>
            <label>フレーバー</label>
            <input type="text" name="flavor" value="<%= canele.flavor %>" required>
          </div>
        </div>

        <div class="caneles-formGrid2">
          <div>
            <label>外カリ（1〜10）</label>
            <input type="number" name="crunch" min="1" max="10" value="<%= canele.crunch %>" required>
          </div>
          <div>
            <label>中しっとり（1〜10）</label>
            <input type="number" name="softness" min="1" max="10" value="<%= canele.softness %>" required>
          </div>
        </div>

        <div class="caneles-formGrid2">
          <div>
            <label>甘さ</label>
            <input type="text" name="sweetness" value="<%= canele.sweetness %>" required>
          </div>
          <div>
            <label>総合スコア（1〜10）</label>
            <input type="number" name="score" min="1" max="10" value="<%= canele.score %>" required>
          </div>
        </div>

        <label>フレーバーメモ</label>
        <textarea name="notes"><%= canele.notes %></textarea>

        <label>コメント</label>
        <textarea name="comment"><%= canele.comment %></textarea>

        <div class="caneles-footerActions">
          <a href="/caneles/<%= canele.id %>" class="caneles-btn">戻る</a>
          <button type="submit" class="caneles-btn primary">更新</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
